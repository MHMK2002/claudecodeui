/**
 * Quick Settings tab — wraps the existing QuickSettingsBody so users
 * can manage preferences from inside the new right sidebar.
 */

import QuickSettingsBody from '../../quick-settings-panel/view/QuickSettingsBody';

export function QuickSettingsTab() {
  return <QuickSettingsBody />;
}